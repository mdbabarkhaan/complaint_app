import 'package:flutter/material.dart';

class AppColors{
  static final lightRedColor = Colors.red.withOpacity(0.4);
  static final darkRedColor = Colors.red.withOpacity(0.8);
  static const whiteColor = Colors.white;
}